(function(){

  angular
       .module('poverty')
       .controller('DashboardController', [
          '$log', '$q',
          DashboardController
       ]);

  /**
   * Main Controller for the Angular Material Starter App
   * @param $scope
   * @param $mdSidenav
   * @param avatarsService
   * @constructor
   */
  function DashboardController($log, $q) {
    var vm = this;
  }

})();
